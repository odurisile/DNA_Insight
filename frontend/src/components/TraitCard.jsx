import React from 'react';
import { Card, CardContent, Typography, LinearProgress } from "@mui/material";
export default function TraitCard({title,result}){return(<Card><CardContent>{title}</CardContent></Card>);}